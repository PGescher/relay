{
  "name": "relay-monorepo",
  "private": true,
  "scripts": {
    "dev": "docker-compose up",
    "build": "pnpm -r build",
    "db:migrate": "pnpm --filter @relay/api prisma migrate dev"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}